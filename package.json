{
  "name": "defined-by-dwija",
  "version": "1.0.0",
  "private": true,
  "description": "Static Defined by Dwija website with a Vercel Serverless inquiry endpoint.",
  "scripts": {
    "check": "node --check script.js && node --check api/inquiry.js",
    "start": "python -m http.server 4173 --bind 127.0.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}