{
  "name": "defined-by-dwija",
  "version": "1.0.0",
  "private": true,
  "description": "Static Defined by Dwija website with a Vercel Serverless inquiry endpoint.",
  "scripts": {
    "check": "node --check script.js && node --check api/inquiry.js && node --check dev-server.js",
    "dev": "node dev-server.js",
    "start": "node dev-server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
